1. 給我現在目前的Context + 數據字典可以下載的MD 文件，新對話框貼上後你能完全了解目前項目進度結構，不同模塊間的關聯。 需要將優先繼續的和功能需要完善的和其他bug 也總結到context, 這樣我們好繼續。
2. 在給我個進度記錄

完整文件結構（所有組件路徑）
數據字典（14 個主要表，所有欄位）
系統架構（登入流程、平台進入公司、郵件系統）
已完成功能清單
待完成功能（按優先順序）
已知 Bug 狀態
重要帳號信息
App.jsx 關鍵 State 和函數
下次對話建議起點



# HR SaaS 項目 Context v8.0
> 最後更新：2026-04-23
> 新對話框貼上此文件後可完全了解項目狀態

---

## 一、項目基本信息

### 技術棧
- **前端**：React + Vite 5.4.0 + Tailwind CSS 4.2.1
- **後端**：Supabase (PostgreSQL + Auth + Edge Functions)
- **郵件**：Resend API（域名 felihr.com）
- **部署**：Vercel
- **本地路徑**：`C:\Users\Felix\Documents\AI project\AI programing\HR saas`
- **GitHub**：https://github.com/felixzhao301-sg/HR-saas
- **Vercel**：https://hr-saas-nine.vercel.app

### Supabase 項目
- **Project ID**：fwztdcparxtnfffkkajh
- **Project URL**：https://fwztdcparxtnfffkkajh.supabase.co

### 公司 ID
| 公司 | ID |
|------|----|
| YLL OFFSHORE SERVICES PTE. LTD. | `57d502e9-206c-4333-82d3-78361875ca28` |
| Cactus Group Pte. Ltd. | `a3581da0-c7ee-459b-8087-69b7083dd8e8` |
| Co. TEST1 | 測試公司 |

---

## 二、文件結構

```
src/
├── assets/
├── components/
│   ├── AttachmentField.jsx
│   ├── AttachmentLink.jsx
│   ├── CPFReportModal.jsx
│   ├── Field.jsx
│   ├── IR8AModal.jsx           ← 打印彈窗
│   ├── LoginPage.jsx
│   ├── Navbar.jsx
│   ├── PayrollSummaryModal.jsx
│   ├── PayslipModal.jsx        ← 工資單打印（用 createPortal）
│   ├── ResignModal.jsx         ← 離職彈窗
│   ├── ResetPasswordPage.jsx
│   ├── SubTableForm.jsx
│   └── Field.jsx
├── i18n/
│   └── index.js                ← zh/en/ms 三語言
├── pages/
│   ├── PlatformAdminPage.jsx   ← 平台管理（含 Companies + Staff）
│   └── RegisterPage.jsx
├── tabs/
│   ├── CommissionTab.jsx
│   ├── CompanyDocumentsTab.jsx
│   ├── DashboardTab.jsx
│   ├── DropdownSettingsTab.jsx
│   ├── EmployeeDetailPage.jsx  ← 員工詳情頁（sticky header + iOS profile）
│   ├── EmployeesTab.jsx        ← 員工列表（~309行）
│   ├── IR8ATab.jsx
│   ├── LeaveApproversTab.jsx
│   ├── LeaveManagementTab.jsx
│   ├── LeaveTypesTab.jsx
│   ├── MyLeaveTab.jsx
│   ├── PayrollTab.jsx
│   ├── PermissionsTab.jsx
│   ├── PersonalDocsTab.jsx
│   ├── SettingsTab.jsx
│   ├── UserManagementTab.jsx
│   ├── WorkPassTab.jsx         ← EP/SP/WP 工作準證（多附件）
│   └── employee/               ← 員工詳情子模塊
│       ├── BulkUploadModal.jsx
│       ├── DependentsTab.jsx
│       ├── EducationTab.jsx
│       ├── FormFields.jsx      ← hasAccount prop 控制 work_email 只讀
│       ├── LeaveTab.jsx        ← 員工假期（已修復批准人郵件查詢）
│       ├── MedicalTab.jsx
│       ├── RecordsTab.jsx      ← 組合 WorkHistory+Education+Medical+Dependents+Visa
│       ├── VisaInlineTab.jsx   ← 入境簽證（在 Records Tab 裡）
│       └── WorkHistoryTab.jsx
├── utils/
│   ├── attachments.js
│   ├── auth.js
│   ├── email.js
│   ├── guard.js                ← isPlatformAdmin()
│   ├── payroll.js              ← CPF 計算引擎
│   ├── permissions.js
│   ├── register.js
│   └── subscription.js
├── App.jsx                     ← 主路由（已加 handleEnterCompany + enteredFromPlatform）
├── App.css
├── constants.js
├── index.css
├── main.jsx
└── supabase.js

supabase/
└── functions/
    └── send-email/
        └── index.ts            ← Edge Function（本地已更新但未部署）
```

---

## 三、數據庫表結構（數據字典）

### companies
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid | PK |
| name | text | 公司名稱 |
| uen | text | 新加坡 UEN |
| address | text | 地址 |
| postal_code | text | 郵編 |
| phone | text | 電話 |
| industry | text | 行業 |
| status | text | active/pending/suspended |
| plan | text | trial/basic/pro/enterprise |
| trial_ends_at | timestamptz | 試用到期 |
| subscription_ends_at | timestamptz | 訂閱到期 |
| max_employees | integer | 最大員工數 |
| stripe_customer_id | text | Stripe 客戶 ID |
| created_at | timestamptz | 創建時間 |

### employees
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid | PK |
| company_id | uuid | FK → companies |
| auth_user_id | uuid | FK → auth.users |
| full_name | text | 姓名 |
| date_of_birth | date | 出生日期 |
| gender | text | male/female |
| nationality | text | 國籍 |
| race | text | 種族 |
| nric_fin | text | NRIC/FIN |
| address | text | 地址 |
| personal_mobile | text | 個人手機 |
| personal_email | text | 個人電郵 |
| work_email | text | 公司電郵（登入帳號，有帳號後只讀） |
| passport_no | text | 護照號碼 |
| passport_issue_date | date | 護照發出日 |
| passport_expiry_date | date | 護照到期日 |
| join_date | date | 入職日期 |
| employment_type | text | full_time/part_time |
| position | text | 職位 |
| is_pr | boolean | 是否 PR |
| pr_year | text | PR 年份（1/2/3+） |
| is_seaman | boolean | 是否海員 |
| seaman_no | text | 海員證號 |
| seaman_expiry | date | 海員證到期 |
| basic_salary | numeric | 基本工資 |
| basic_allowance | numeric | 基本津貼 |
| annual_leave | integer | 年假天數 |
| bank_name | text | 銀行名稱 |
| bank_country | text | 銀行國家 |
| bank_account_no | text | 銀行帳號 |
| bank_account_name | text | 銀行帳戶名 |
| bank_remarks | text | 銀行備注 |
| **status** | text | **active/resigned（新增）** |
| **resign_date** | date | **離職日期（新增）** |
| **resign_reason** | text | **離職原因（新增）** |
| created_at | timestamptz | 創建時間 |

### user_roles
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid | PK |
| user_id | uuid | FK → auth.users |
| role | text | 角色（見角色列表）|
| display_name | text | 顯示名稱 |
| email | text | 登入電郵 |
| company_id | uuid | FK → companies（平台角色為 NULL）|
| employee_id | uuid | FK → employees（可選）|
| created_at | timestamptz | 創建時間 |

**角色列表：**
- 公司角色：`super_admin`, `hr_admin`, `hr_staff`, `manager`, `employee`, `finance`, `read_only`
- 平台角色：`platform_admin`, `platform_staff`

### leave_approvers
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid | PK |
| employee_id | uuid | FK → employees（UNIQUE）|
| company_id | uuid | FK → companies |
| approver1_user_id | uuid | 第一批准人 auth user id |
| approver2_user_id | uuid | 第二批准人 auth user id（可選）|
| require_both | boolean | 是否需要雙批 |

### leave_applications
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid | PK |
| employee_id | uuid | FK → employees |
| leave_type | text | annual/medical/unpaid/public_holiday/others |
| start_date | date | 開始日期 |
| end_date | date | 結束日期 |
| days | numeric | 天數 |
| reason | text | 原因 |
| status | text | pending/approved/rejected |
| attachment_url | text | 附件 URL |
| created_at | timestamptz | 創建時間 |

### leave_balances
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid | PK |
| employee_id | uuid | FK → employees |
| company_id | uuid | FK → companies |
| year | integer | 年份 |
| leave_type | text | 假期類型 |
| entitled | numeric | 應得天數 |
| carried_forward | numeric | 結轉天數 |
| adjusted | numeric | 額外調整 |
| used | numeric | 已用天數 |
| UNIQUE | - | (employee_id, year, leave_type) |

### payroll_runs
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid | PK |
| company_id | uuid | FK → companies |
| year | integer | 年份 |
| month | integer | 月份 |
| status | text | draft/finalized |
| finalized_at | timestamptz | 確認時間 |
| created_at | timestamptz | 創建時間 |

### payroll_records
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid | PK |
| payroll_run_id | uuid | FK → payroll_runs |
| employee_id | uuid | FK → employees |
| basic_salary | numeric | 基本工資快照 |
| basic_allowance | numeric | 基本津貼快照 |
| commission | numeric | 佣金 |
| bonus | numeric | 獎金 |
| other_income | numeric | 其他收入 |
| cpf_employee | numeric | 員工 CPF |
| cpf_employer | numeric | 僱主 CPF |
| sdl | numeric | SDL |
| deductions | numeric | 其他扣除 |
| net_pay | numeric | 實發工資 |
| gross_pay | numeric | 總薪資 |

### company_payroll_settings
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid | PK |
| company_id | uuid | FK → companies（UNIQUE）|
| working_days | integer | 每月工作天數（默認 26）|
| sdl_rate | numeric | SDL 比率 |
| include_allowance_in_cpf | boolean | 津貼是否計入 CPF |

### commissions
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid | PK |
| employee_id | uuid | FK → employees |
| company_id | uuid | FK → companies |
| year | integer | 年份 |
| month | integer | 月份 |
| amount | numeric | 金額 |
| description | text | 說明 |
| created_at | timestamptz | 創建時間 |

### employee_visa（工作準證 Work Pass）
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid | PK |
| employee_id | uuid | FK → employees |
| visa_type | text | EP/SP/WP/DP/LTVP/PEP/EntrePass/Others |
| visa_number | text | 準證號碼 |
| **application_date** | date | **申請日期（新增）** |
| issue_date | date | 發出日期 |
| expiry_date | date | 到期日期 |
| issued_by | text | 發出機構（MOM）|
| remarks | text | 備注 |
| attachment_url | text | 舊版單附件（已廢棄，用 attachments 表）|

### employee_visa_attachments（Work Pass 多附件）
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid | PK |
| visa_id | uuid | FK → employee_visa |
| file_name | text | 文件名 |
| attachment_url | text | 文件 URL |
| created_at | timestamptz | 創建時間 |

### employee_personal_docs（個人文件）
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid | PK |
| employee_id | uuid | FK → employees |
| company_id | uuid | FK → companies |
| doc_type | text | 文件類型 |
| file_name | text | 文件名 |
| attachment_url | text | 文件 URL |
| remarks | text | 備注 |
| created_at | timestamptz | 創建時間 |

### employee_documents（公司信件/文件）
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid | PK |
| employee_id | uuid | FK → employees |
| company_id | uuid | FK → companies |
| created_by | uuid | FK → auth.users |
| doc_type | text | offer_letter/increment/warning/others |
| title | text | 標題 |
| content | text | 內容 |
| attachment_url | text | 附件 URL |
| created_at | timestamptz | 創建時間 |

### employee_work_history / employee_education / employee_medical / employee_dependents
標準子表，各有 `employee_id` FK，不詳列。

### dropdown_options
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid | PK |
| company_id | uuid | FK → companies |
| category | text | race 等 |
| value | text | 選項值 |
| label_zh | text | 中文標籤 |
| label_en | text | 英文標籤 |
| sort_order | integer | 排序 |

---

## 四、系統架構

### 登入流程
```
用戶登入
  → isPlatformAdmin(userId)?
      → YES: 顯示 PlatformAdminPage
      → NO: 查 user_roles → 獲取 role + company_id
          → checkCompanyValid() → 檢查 status/trial/subscription
              → 無效: BlockedScreen
              → 有效: 主系統
```

### 平台 Admin 進入公司
```
PlatformAdminPage → 點 🚀 Enter
  → handleEnterCompany(company) in App.jsx
      → setIsPlatformAdminMode(false)
      → setEnteredFromPlatform(true)
      → setCompanyId / setUserRole('super_admin')
  → 頂部 Navbar 顯示黃色 ← Platform 按鈕
```

### 郵件系統
- **服務**：Resend API
- **發件人**：`HR SaaS <noreply@felihr.com>`
- **Edge Function**：`supabase/functions/send-email/index.ts`
- **郵件類型**：
  - `password_reset` — 重設密碼
  - `user_invited` — 帳號創建邀請（公司員工 + 平台員工）
  - `new_company_registered` — 新公司註冊通知
  - `leave_submitted` — 假期申請通知批准人
  - `leave_approved` — 假期批准通知
  - `leave_rejected` — 假期拒絕通知

### CPF 計算
- 文件：`src/utils/payroll.js`
- 馬來西亞員工無 CPF（正確行為）
- PR 員工按年份有不同比率

---

## 五、員工詳情頁面結構

### Tab 結構
| Tab | 內容 | 組件 |
|-----|------|------|
| 👤 資料 | 基本資料/工作資料/護照/銀行（iOS 設置風格） | EmployeeDetailPage.jsx |
| 🌏 工作準證 | EP/SP/WP 等（多附件） | WorkPassTab.jsx |
| 📁 個人文件 | CV/護照/地址證明等上傳 | PersonalDocsTab.jsx |
| 📋 記錄 | 工作經歷/學歷/醫療/家屬/簽證 | RecordsTab.jsx |
| 🏢 公司文件 | 入職信/加薪信/警告信等 | CompanyDocumentsTab.jsx |
| 🌴 假期 | 餘額/申請/記錄 | LeaveTab.jsx |

### 重要設計決策
- `work_email` 有帳號後只讀（`hasAccount` prop）
- Work Pass = EP/SP/WP 工作準證（WorkPassTab）
- Visa = 入境簽證 Tourist/Business 等（VisaInlineTab，在 Records 裡）
- 員工離職用 Soft Delete（`status = 'resigned'`，保留薪資快照）

---

## 六、平台管理頁面結構

### Tab 結構
| Tab | 功能 |
|-----|------|
| 🏢 Companies | 公司列表、詳情查看、編輯、Activate/Suspend |
| 👥 Staff | 平台員工管理（Admin/Staff 角色）|

### 平台角色權限
| 角色 | 權限 |
|------|------|
| `platform_admin` | 全部功能，管理員工和公司 |
| `platform_staff` | 只能查看公司、進入公司系統 |

### 平台員工帳號
| 人員 | Email | 角色 |
|------|-------|------|
| Felix Zhao | felix.zhao301@gmail.com | platform_admin |
| Lee Wun | leewun.ang@gmail.com | platform_staff |

---

## 七、已完成功能清單

### ✅ 核心功能
- [x] 多公司 SaaS 架構
- [x] 平台 Admin / 公司 Admin 分層
- [x] 試用期管理（Banner + BlockedScreen）
- [x] 三語言（中文/英文/馬來文）
- [x] 角色權限系統
- [x] 員工 CRUD + Soft Delete 離職
- [x] 批量上傳員工（Excel/CSV）
- [x] 工作準證管理（多附件）
- [x] 個人文件管理
- [x] 公司文件管理
- [x] 工作記錄/學歷/醫療/家屬/簽證
- [x] 假期申請 + 批准人通知郵件
- [x] 假期餘額管理
- [x] 薪資計算（CPF + SDL）
- [x] 工資單打印
- [x] 佣金管理
- [x] IR8A Tab
- [x] 忘記密碼郵件
- [x] 用戶管理（創建/編輯/角色）
- [x] 平台公司詳情+編輯
- [x] 平台員工管理
- [x] 進入公司系統（平台→公司）
- [x] 員工 Profile iOS 設置風格 + Sticky Header

---

## 八、待完成功能（優先順序）

### 🔴 高優先
1. **假期批准/拒絕功能**
   - `LeaveManagementTab.jsx` 需要加批准/拒絕按鈕
   - 批准後更新 `leave_balances.used`
   - 發郵件通知員工（`leave_approved` / `leave_rejected`）

2. **IR8A 年度生成**
   - `IR8ATab.jsx` 已有 UI
   - 需要實際生成邏輯（從 payroll_records 彙總）
   - 打印功能（IR8AModal.jsx）

3. **Edge Function 部署**
   - 本地 `index.ts` 已更新（修復 `companyId: null` 問題）
   - 需要安裝 Supabase CLI 並部署
   - 或通過 GitHub Actions 自動部署
   - 影響：公司員工邀請郵件目前只有 role=employee 才發

4. **年度結算流程**
   - 結轉年假（carried_forward）
   - 清除舊年度已離職員工
   - IR8A 批量生成

### 🟡 中優先
5. **薪資工作日設置 UI**
   - `company_payroll_settings.working_days` 已有欄位
   - 需要在 SettingsTab 加 UI

6. **兼職薪資計算**
   - 按工作天數計算
   - 需要在 PayrollTab 加邏輯

7. **LeaveManagementTab 改善**
   - 目前缺少批量操作
   - 需要加按月/按假期類型篩選

8. **報告功能（平台）**
   - 各公司員工數統計
   - 訂閱到期提醒
   - 活躍度統計

### 🔵 低優先
9. **Supabase Storage bucket `company-assets`**
   - 公司 Logo 上傳

10. **手機號登錄**
    - 暫時不做，等 Phase 2

11. **Stripe 接入**
    - 自動收費

---

## 九、已知 Bug

| Bug | 狀態 | 說明 |
|-----|------|------|
| Edge Function `companyId: null` | ⚠️ 本地已修，未部署 | 平台員工邀請郵件走前端繞過 |
| LeaveApproversTab 保存後需刷新 | ✅ 已修 | 改了 cancelEdit/loadData 順序 |
| Work Pass Tab 文件路徑 | ✅ 已修 | 移到 src/tabs/ |
| 假期申請批准人查詢 | ✅ 已修 | 改用 employee_id 而非 leave_type |
| PayslipModal 打印空白 | ✅ 已修 | 用 createPortal |
| Anderson 重複帳號 | ✅ 已清理 | 保留 anderson@ylloffshore.com |

---

## 十、重要帳號信息

### 測試帳號
| 帳號 | 密碼 | 角色 |
|------|------|------|
| fzhao@ylloffshore.com | （已設定）| YLL super_admin |
| felix.zhao301@gmail.com | （已設定）| platform_admin |
| anderson@ylloffshore.com | （已設定）| YLL employee |
| leewun.ang@gmail.com | （已設定）| platform_staff |

### YLL 員工 Auth User IDs（重要）
- Felix Zhao (fzhao): `75dc2b39-1801-4a5a-bde1-420e0a752928`
- Anderson (employee): `741cfe71-c15a-469a-ac7f-4a619c940400`

---

## 十一、App.jsx 關鍵 State

```js
// 平台相關
const [isPlatformAdminMode, setIsPlatformAdminMode] = useState(false)
const [enteredFromPlatform, setEnteredFromPlatform] = useState(false)

// 公司相關
const [companyId, setCompanyId] = useState(null)
const [companyName, setCompanyName] = useState('')
const [userRole, setUserRole] = useState(null)
const [permissions, setPermissions] = useState({})

// 試用期
const [trialDaysLeft, setTrialDaysLeft] = useState(null)
const [companyBlocked, setCompanyBlocked] = useState(false)
const [blockReason, setBlockReason] = useState(null)
```

### handleEnterCompany（平台進入公司）
```js
async function handleEnterCompany(company) {
  setIsPlatformAdminMode(false)
  setEnteredFromPlatform(true)
  setCompanyId(company.id)
  setCompanyName(company.name)
  setUserRole('super_admin')
  setUserDisplayName('Platform Admin')
  const perms = await loadPermissions(company.id, 'super_admin')
  setPermissions(perms)
  fetchRaceOptions()
  setMainTab('dashboard')
  setTrialDaysLeft(null)
  setCompanyBlocked(false)
}
```

---

## 十二、下一步建議

**下次對話從這裡開始：**

1. **假期批准/拒絕功能**（最重要，因為用戶已能申請但無法批准）
2. **IR8A 生成邏輯**
3. **Edge Function 部署**（安裝 Supabase CLI）
4. **年度結算流程**

---

*Context v8.0 — 2026-04-23*