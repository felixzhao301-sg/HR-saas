ok, 我們現在把這個推上網。 然後再 
1.結合今天我們改的準備目前的Context + 數據字典，可以下載的MD 文件，新對話框貼上後你能完全了解目前項目進度結構，不同模塊間的關聯。 需要將優先繼續的和功能需要完善的和其他bug 也總結到context, 這樣我們好繼續。 
2. 在給我個進度記錄。3. 一份完整的使用說明，中英文對照

完整文件結構（所有組件路徑）
數據字典（14 個主要表，所有欄位）
系統架構（登入流程、平台進入公司、郵件系統）
已完成功能清單
待完成功能（按優先順序）
已知 Bug 狀態
重要帳號信息
App.jsx 關鍵 State 和函數
下次對話建議起點



# HR SaaS Context v9.1 — 完整項目文檔
更新日期：2026-04-27

---

## 一、項目基本信息

- **項目名稱**: FelihR — HR SaaS 人力資源管理系統
- **技術棧**: React + Vite 5.4.0 + Tailwind CSS + Supabase + Vercel
- **本地路徑**: `C:\Users\Felix\Documents\AI project\AI programing\HR saas`
- **GitHub**: https://github.com/felixzhao301-sg/HR-saas
- **Vercel (生產)**: https://hr-saas-nine.vercel.app
- **Supabase Project ID**: fwztdcparxtnfffkkajh
- **Stripe 帳號**: FELIANN SINGAPORE PTE（已開通，目前在 Sandbox 測試模式）
- **郵件服務**: Resend（smtp.resend.com，noreply@felihr.com）

---

## 二、系統架構

### 用戶角色體系
```
platform_admin  → 平台管理員（管理所有公司）
platform_staff  → 平台工作人員（只讀公司信息）
─────────────────────────────────────────
super_admin     → 公司最高管理員
hr_admin        → HR 管理員
hr_staff        → HR 職員
manager         → 部門經理
finance         → 財務
employee        → 普通員工
read_only       → 只讀
```

### 分離型設計（v9.1 確定）
- **Admin 和 Employee 完全分離**
- `user_roles` 表 = 登入帳號和角色
- `employees` 表 = 員工詳細資料（需薪資/假期功能才需建立）
- Super Admin 不自動建立 Employee Profile
- 如 Admin 自己也是員工（需工資單），手動在員工管理新增

### 註冊流程（v9.1 確定）
```
客戶填表 → 建帳號 + 建公司(status=trial)
→ Supabase 自動發確認郵件（Resend）
→ 客戶點郵件確認連結
→ 直接登入，開始 30 天試用
→ 試用期結束 → Stripe 付款 → 繼續使用
```

### 主要模塊
```
App.jsx
├── PlatformAdminPage（平台後台）
│   ├── CompaniesTab
│   ├── PlatformStaffTab
│   └── PlatformAnnouncementsTab
│
└── 公司端（主 App）
    ├── DashboardTab（首頁/公告）
    ├── EmployeesTab（員工管理）
    │   ├── EmployeeDetailPage
    │   ├── FormFields
    │   └── BulkUploadModal
    ├── MyLeaveTab
    ├── LeaveManagementTab
    ├── PayrollTab（含 MobileEditPage）
    ├── CommissionTab
    ├── IR8ATab
    ├── YearEndTab（設定菜單）
    └── 設定類
        ├── SettingsTab
        ├── UserManagementTab
        ├── LeaveTypesTab
        ├── LeaveApproversTab
        ├── DropdownSettingsTab
        └── PermissionsTab
```

---

## 三、數據庫表結構（數據字典）

### 3.1 平台層

#### `companies`
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid PK | |
| name | text | 公司名稱 |
| uen | text | 新加坡UEN |
| address | text | |
| postal_code | text | |
| phone | text | |
| industry | text | |
| status | text | **trial**/active/pending/suspended |
| plan | text | trial/starter/growth/business |
| max_employees | int | 預設20 |
| trial_ends_at | date | 試用期結束（30天） |
| subscription_ends_at | date | 訂閱結束日 |
| created_at | timestamptz | |

#### `platform_announcements`
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid PK | |
| title | text | |
| content | text | |
| type | text | info/warning/update |
| is_active | boolean | |
| expires_at | date | |
| created_by | uuid → auth.users | |
| created_at | timestamptz | |

#### `subscription_plans` ⭐（已建表，待填數據）
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | text PK | starter/growth/business |
| name | text | |
| price_monthly | numeric | SGD |
| price_yearly | numeric | SGD |
| max_employees | int | |
| stripe_price_id_monthly | text | 待填 |
| stripe_price_id_yearly | text | 待填 |
| features | jsonb | |
| is_active | boolean | |

#### `subscriptions` ⭐（已建表）
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid PK | |
| company_id | uuid | |
| plan_id | text | |
| status | text | active/past_due/cancelled/trialing |
| billing_cycle | text | monthly/yearly |
| stripe_subscription_id | text | |
| stripe_customer_id | text | |
| current_period_start | timestamptz | |
| current_period_end | timestamptz | |
| cancel_at_period_end | boolean | |

#### `invoices` ⭐（已建表）
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid PK | |
| company_id | uuid | |
| subscription_id | uuid | |
| stripe_invoice_id | text | |
| amount | numeric | |
| currency | text | sgd |
| status | text | paid/open/void |
| invoice_pdf | text | |
| paid_at | timestamptz | |

---

### 3.2 員工層

#### `employees`（⚠️ 所有欄位現在允許 NULL）
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid PK | |
| company_id | uuid | |
| auth_user_id | uuid → auth.users | 關聯登入帳號（可NULL） |
| full_name | text | |
| nric_fin | text | |
| date_of_birth | date | |
| gender | text | male/female |
| nationality | text | |
| race | text | |
| join_date | date | |
| resign_date | date | |
| employment_type | text | full_time/part_time |
| position | text | |
| status | text | active/resigned |
| basic_salary | numeric | |
| basic_allowance | numeric | |
| annual_leave | int | |
| is_pr | boolean | |
| pr_year | text | 1/2/3+ |
| is_seaman | boolean | |
| seaman_no | text | |
| seaman_expiry | date | |
| passport_no | text | |
| passport_issue_date | date | |
| passport_expiry_date | date | |
| personal_email | text | 假期通知用 |
| work_email | text | 登入帳號用 |
| personal_mobile | text | |
| address | text | |
| bank_name | text | |
| bank_account_no | text | |
| bank_account_name | text | |
| bank_country | text | |
| bank_remarks | text | |
| ir21_filed | boolean | |
| ir21_filed_date | date | |

#### `user_roles`
| 欄位 | 類型 | 說明 |
|------|------|------|
| user_id | uuid PK | |
| company_id | uuid | 平台角色此欄為NULL |
| role | text | 見角色體系 |
| display_name | text | |
| email | text | |
| created_at | timestamptz | |

---

### 3.3 假期層

#### `leave_types`
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid PK | |
| company_id | uuid | |
| name | text | |
| is_active | boolean | |
| carry_forward_days | int | 0=不結轉 |
| carry_forward_expiry_months | int | |

#### `leave_balances`
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid PK | |
| employee_id | uuid | |
| company_id | uuid | |
| year | int | |
| leave_type | text | annual/medical/unpaid/others |
| entitled | numeric | |
| carried_forward | numeric | |
| adjusted | numeric | |
| used | numeric | |

#### `leave_applications`
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid PK | |
| employee_id | uuid | |
| company_id | uuid | |
| leave_type | text | |
| start_date | date | |
| end_date | date | |
| days | numeric | |
| reason | text | |
| status | text | pending/approved/rejected |
| approved_by | uuid | auth_user_id |
| approved_at | timestamptz | |
| rejection_reason | text | |

---

### 3.4 薪資層

#### `payroll_runs`
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid PK | |
| company_id | uuid | |
| year, month | int | |
| status | text | draft/confirmed/locked |
| working_days | int | |
| payment_date | date | |
| locked_at | timestamptz | |
| locked_by | uuid | |

#### `payroll_records`（含 snap_* 快照欄位和 ytd_* YTD欄位）
主要欄位：basic_salary, fixed_allowance, other_allowance, overtime, bonus, commission, director_fee, unutilised_leave_pay, unpaid_leave_days, unpaid_leave_amount, gross_salary, employee_cpf, employer_cpf, cdac_mbmf_sinda, sdl, fwl, net_pay, other_adjustments(jsonb), other_adjustments_total, net_pay_after_adjustments, total_cpf, total_company_paid

YTD欄位（鎖定時寫入）：ytd_basic, ytd_fixed_allowance, ytd_other_allowance, ytd_overtime, ytd_bonus, ytd_commission, ytd_director_fee, ytd_gross, ytd_employee_cpf, ytd_employer_cpf, ytd_sdl, ytd_fwl, ytd_net_pay, ytd_total_company_paid

#### `commissions`
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid PK | |
| employee_id, company_id | uuid | |
| year, month | int | |
| amount | numeric | |
| status | text | draft/confirmed |

---

### 3.5 公告層

#### `announcements`（公司公告）
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | uuid PK | |
| company_id | uuid | |
| title, content | text | |
| is_pinned | boolean | |
| created_by | uuid | |
| created_by_name | text | |
| expires_at | date | |

---

## 四、Edge Functions

### `send-email` 支援的 actions
| action | 說明 |
|--------|------|
| create-user | 建立 auth 帳號 + user_roles + 發邀請郵件 |
| delete-user | 刪除 auth.users |
| notify-admin | 通知平台管理員新公司註冊 |

部署：`supabase functions deploy send-email --project-ref fwztdcparxtnfffkkajh`

---

## 五、Supabase 設置（已確認）

| 設置 | 狀態 |
|------|------|
| Site URL | https://hr-saas-nine.vercel.app |
| Redirect URLs | vercel + localhost 各兩個 |
| Confirm email | ✅ 已開啟 |
| Custom SMTP | ✅ Resend (smtp.resend.com) |
| 發件人 | noreply@felihr.com / HR SaaS |

---

## 六、業務規則

| 規則 | 說明 |
|------|------|
| 登入帳號 | work_email |
| 假期通知郵件 | personal_email |
| 密碼重設郵件 | Supabase 自動發到 work_email |
| 註冊確認郵件 | Supabase 自動發（Resend） |
| 新公司 | 註冊後直接 trial，30天試用 |
| IR21 vs IR8A | 外籍員工離職做IR21，IR21員工從IR8A CSV排除 |
| 年度清除時機 | IR8A提交IRAS後清除離職員工 |
| 公司公告權限 | super_admin/hr_admin 才能發布/刪除 |
| 薪資匯總 | 只有 super_admin/hr_admin/finance 可查看 |
| 用戶管理 | 只顯示 status=active 員工 |
| 刪除帳號 | 同步刪 user_roles + auth.users + 清空 auth_user_id |
| YTD寫入 | 薪資鎖定（Lock）時寫入 ytd_* 欄位 |
| Admin/Employee | 分離型，Admin 不自動建 Employee Profile |

---

## 七、文件路徑

```
src/
├── App.jsx
├── constants/index.js         ← EMPLOYEE_COLUMNS + EMPLOYEE_COLUMNS_EXAMPLE
├── pages/
│   ├── PlatformAdminPage.jsx  ← 含公告Tab
│   └── RegisterPage.jsx       ← 3步驟，UEN驗證
├── components/
│   ├── PayrollSummaryModal.jsx ← Excel + YTD Tab
│   ├── ResignModal.jsx         ← IR21欄位
│   └── IR8AModal.jsx
├── tabs/
│   ├── DashboardTab.jsx        ← 平台公告 + 公司公告權限
│   ├── EmployeesTab.jsx        ← 勾選式清除
│   ├── EmployeeDetailPage.jsx
│   ├── LeaveManagementTab.jsx  ← 批准/拒絕 + 郵件
│   ├── PayrollTab.jsx          ← 批量輸入 + 手機獨立編輯
│   ├── IR8ATab.jsx             ← Excel + IRAS CSV
│   ├── YearEndTab.jsx          ← 年假結轉
│   ├── UserManagementTab.jsx
│   └── employee/
│       ├── FormFields.jsx
│       └── BulkUploadModal.jsx
└── utils/
    ├── payroll.js              ← CPF/SDL/FWL計算
    ├── register.js             ← 註冊流程（trial模式）
    ├── permissions.js
    ├── subscription.js
    └── attachments.js

supabase/functions/send-email/index.ts
```

---

## 八、待辦事項（優先順序）

### 🔴 高優先（下次對話繼續）

#### 付款模塊（進行中）
- [ ] Stripe Dashboard 建立套餐產品（Starter/Growth/Business）
- [ ] 拿到 Price IDs → 填入 `subscription_plans` 表
- [ ] 拿到 API Keys（pk_test / sk_test）
- [ ] sk_test 加入 Supabase Edge Function 環境變量
- [ ] 建立 `stripe-webhook` Edge Function
- [ ] 客戶自助訂閱頁面（前端）
  - 套餐選擇（月付/年付切換）
  - Stripe Checkout + PayNow
  - 付款成功 → 自動更新 company.status + subscription_ends_at
- [ ] 訂閱管理頁（查看套餐、發票、取消）

#### 定價（確認後填入數據庫）
| 套餐 | 員工上限 | 月費 | 年費 |
|------|----------|------|------|
| Starter | 10人 | S$49 | S$470 |
| Growth | 30人 | S$99 | S$950 |
| Business | 100人 | S$199 | S$1,910 |

### 🟡 中優先

- [ ] `send-email` Edge Function 加 `notify-admin` action（現在 401）
- [ ] IR8A 手動填入備用方案
- [ ] PayrollSummaryModal YTD 測試
- [ ] 邀請郵件功能測試
- [ ] 佣金計算模塊完善

### 🟢 Backlog

- [ ] 兼職薪資計算
- [ ] 工資單 PDF 直接下載
- [ ] 員工自助申請加班/調班
- [ ] 報表中心

---

## 九、已知問題

| 問題 | 狀態 | 說明 |
|------|------|------|
| RegisterPage 內容混入 ResignModal | ✅已修復 | 人為錯誤，已恢復 |
| register.js 自動建 Employee Profile | ✅已修復 | 分離型設計，已移除 |
| Confirm email 未開啟導致422 | ✅已修復 | Supabase已開啟 |
| notify-admin Edge Function 401 | ⚠️待修復 | 未登入狀態調用需 service role |
| employees 表 NOT NULL 約束 | ⚠️已放寬 | 所有欄位現允許NULL，前端驗證補充 |
| Salley/wang chuan 資料不完整 | ✅已修復 | 已補填銀行等必要資料 |

---

## 十、Stripe 整合（下次對話從這裡開始）

```
步驟：
1. Stripe Dashboard → Product catalog → 建3個套餐
2. 每個套餐建月付/年付兩個 Price → 拿 Price IDs
3. 填入 subscription_plans 表
4. 拿 pk_test + sk_test
5. sk_test → Supabase Edge Function 環境變量
6. 建 stripe-webhook Edge Function
7. 開發前端訂閱頁面
8. 測試後切換正式 key
```